import { CommandOptions } from "../types/command"

const bootstrap: CommandOptions = {
	name: "bootstrap",
	command: ["bootstrap", ""],
	options: {},
	action: (...args) => {}
}

export default bootstrap